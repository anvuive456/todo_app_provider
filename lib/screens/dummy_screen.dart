import 'package:flutter/material.dart';

class DummyScreen extends StatelessWidget {
  final Widget widget;
  const DummyScreen({Key? key, required this.widget}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return widget;
  }
}
